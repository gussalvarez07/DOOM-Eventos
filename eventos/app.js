// Event handler = Manejadores de eventos
// addEventlistener()
// Conjunto específico de eventos (onclick, onblur, onmouseover, onchange)

/*
function saludar(nombre) {
    alert('Hola ' + nombre);
  }
  
  function despedir(nombre) {
    alert('Adios ' + nombre);
  }
  
  function platicar(nombre) {
    alert('Cuentame como  estás, ' + nombre);
  }
  
  function procesarEntradaUsuario(funcionExtra) {
    const nombre = prompt('Por favor ingresa tu nombre.');
    funcionExtra(nombre);
    //saludar(nombre)
    //despedir(nombre)
    //platicar(nombre)
  }
  
  procesarEntradaUsuario(saludar);
  procesarEntradaUsuario(despedir);
  procesarEntradaUsuario(platicar);*/
  
  // Event handler = Manejadores de eventos
// addEventlistener()
// Conjunto específico de eventos (onclick, onblur, onmouseover, onchange)


function saludar(nombre) {
    alert('Hola ' + nombre);
  }
  
  function despedir(nombre) {
    alert('Adios ' + nombre);
  }
  
  function platicar(nombre) {
    alert('Cuentame como  estás, ' + nombre);
  }
  
  function procesarEntradaUsuario(funcionExtra) {
    const nombre = prompt('Por favor ingresa tu nombre.');
    funcionExtra(nombre);
    //saludar(nombre)
    //despedir(nombre)
    //platicar(nombre)
  }
  
  procesarEntradaUsuario(saludar);
  procesarEntradaUsuario(despedir);
  procesarEntradaUsuario(platicar);
  